# Flutter/Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies

# Android
android/.gradle/
android/local.properties
# 1.9.1: android/key.properties + the release keystore are COMMITTED (stable
# signing key so in-app updates install over the previous release) — the
# ignores that used to hide them from git were removed on purpose.
*.iml

# Generated platform scaffolding that CI (flutter create) will regenerate.
# Committed on purpose: gradle files, manifest, kotlin host — see README.
